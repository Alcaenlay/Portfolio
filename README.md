# Portfolio — Allen Paulo V. Caya

Simple static portfolio site. Open `index.html` in your browser to preview.

Preview locally:

```bash
# Windows (double-click index.html or run):
start index.html

# Or open with a lightweight HTTP server (recommended for some browsers):
python -m http.server 8000
# then open http://localhost:8000
```

To update:
- Edit the content in `index.html`.
- Styles: `assets/styles.css`.
- Project cards: look for the elements inside the `#projects` section. Buttons include `data-title` and `data-desc` attributes used for the modal.
